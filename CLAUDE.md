# Assessment Metadata Registry — Operating Manual

The canonical operating manual is **`AGENTS.md`** (one source of truth, read by every
tool). Claude Code loads it via the import below; if for any reason it is not expanded,
read `./AGENTS.md` directly before doing anything else.

@AGENTS.md
