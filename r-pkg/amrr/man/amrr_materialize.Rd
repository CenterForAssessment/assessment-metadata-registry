% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/materialize.R
\name{amrr_materialize}
\alias{amrr_materialize}
\title{Materialize a resolved metadata object to a binary artifact}
\usage{
amrr_materialize(x, file, name = "amr_metadata")
}
\arguments{
\item{x}{An \code{amrr_metadata} object from \code{\link[=get_metadata]{get_metadata()}}.}

\item{file}{Output path ending in \code{.rds} (single object, read with
\code{\link[=readRDS]{readRDS()}}) or \code{.rda}/\code{.RData} (named object, loaded with \code{\link[=load]{load()}}).}

\item{name}{Object name used inside \code{.rda} files. Default \code{"amr_metadata"}.}
}
\value{
Invisibly, \code{file}.
}
\description{
Persists the object returned by \code{\link[=get_metadata]{get_metadata()}} to \code{.rds} or \code{.rda} for
embedding in another R package (the SGPstateData-style offline pattern).
The artifact carries the registry commit SHA it was derived from
(\code{amrr_registry_ref()}) plus a \code{materialized_at} timestamp, so it is always
traceable back to canonical bytes. It is a derived cache: to update it,
re-query the registry and re-materialize -- never edit the binary.
}
\examples{
\dontrun{
md <- get_metadata("IN", system = "wida-access", year = 2024, registry = ".")
amrr_materialize(md, "wida_in_2024.rda", name = "wida_in_2024")
load("wida_in_2024.rda")
amrr_registry_ref(wida_in_2024)  # the pinned SHA travels with the artifact
}
}
