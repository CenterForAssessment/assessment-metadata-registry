% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build.R
\name{build_registry}
\alias{build_registry}
\title{Build the derived registry layer (Tier B)}
\usage{
build_registry(registry = NULL, out = "build", ddl = NULL, quiet = FALSE)
}
\arguments{
\item{registry}{Path to a registry checkout (containing \verb{metadata/}, \verb{schemas/}).
Defaults to \code{option("amrr.registry")} then \code{AMRR_REGISTRY}.}

\item{out}{Output directory (wiped and rewritten). Default \code{"build"}.}

\item{ddl}{Path to the SQLite DDL. Default \verb{<registry>/schemas/sql/amr-registry.v1.sql}.}

\item{quiet}{Suppress the one-line build summary.}
}
\value{
Invisibly, the manifest list (record counts + provenance stamp).
}
\description{
Reads every authored sidecar under \verb{<registry>/metadata/} and regenerates the
disposable query layer under \code{out}: \code{index.json}, \code{targets.json},
\code{changelog.json}, \verb{tables/*.json}, per-jurisdiction/system \verb{dist/**} bundles,
\code{registry.sqlite} (from the DDL), and a SHA-stamped \code{manifest.json}. Every
bundle carries a \verb{_registry} provenance block. Derived, never canonical.
}
\examples{
\dontrun{
amrr::build_registry(".", out = "build")
}
}
