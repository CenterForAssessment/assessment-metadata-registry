% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_metadata.R
\name{get_metadata}
\alias{get_metadata}
\title{Get assessment metadata for a jurisdiction}
\usage{
get_metadata(
  jurisdiction,
  system = NULL,
  year = NULL,
  registry = NULL,
  ref = NULL,
  attach_targets = TRUE
)
}
\arguments{
\item{jurisdiction}{Jurisdiction id (e.g. \code{"IN"}).}

\item{system}{Optional assessment-system id (e.g. \code{"ilearn"}, \code{"wida-access"}).}

\item{year}{Optional four-digit administration year (character or numeric).}

\item{registry}{One of three forms:
\itemize{
\item \strong{Local checkout} -- a directory containing \verb{metadata/}. If omitted,
\code{amrr} resolves it from \code{option("amrr.registry")}, then the \code{AMRR_REGISTRY}
environment variable, then by \strong{auto-discovery}: it walks up from the
working directory to the nearest checkout (a directory with both \verb{metadata/}
and \verb{schemas/}). So starting R anywhere inside a checkout just works with no
\code{registry} argument; set \code{options(amrr.registry = "/path/to/checkout")} in
your \code{.Rprofile} for a machine-wide default. The pin is git \code{HEAD};
byte-reproducible when you check out a commit SHA.
\item \strong{GitHub repo} -- \code{"github://owner/repo"} (or
\code{"https://github.com/owner/repo"}). Fetches the canonical sidecars for the
jurisdiction straight from GitHub \strong{pinned to an exact commit SHA} (via the
git-trees + raw-content APIs), no checkout required -- a first-class
reproducible remote (ADR-011). Set \code{ref} to the SHA/branch/tag to pin.
Optional auth token from \code{AMRR_GITHUB_TOKEN} / \code{GITHUB_PAT} / \code{GITHUB_TOKEN}
raises the API rate limit; installing \pkg{curl} is recommended.
\item \strong{Derived-layer URL} -- a base URL serving \verb{<registry>/dist/<jur>.json}
(e.g. the published GitHub Pages root); \verb{http://}, \verb{https://}, \verb{file://}.
Convenient but serves the \emph{latest} build only (the derived layer is not
retained per SHA) -- use the GitHub or checkout forms for reproducible pins.
}}

\item{ref}{Optional pin. For the \strong{GitHub} form, \code{ref} is a SHA, branch, or tag
(default: the repo's default-branch \code{HEAD}); it is resolved to a concrete
commit SHA that is fetched and recorded as \code{\link[=amrr_registry_ref]{amrr_registry_ref()}}. For the
local/derived forms, \code{ref} is asserted against the resolved SHA and a mismatch
only warns (the data is read as-is).}

\item{attach_targets}{Merge resolved accountability targets onto each
assessment record? Defaults to \code{TRUE}.}
}
\value{
An object of class \code{amrr_metadata}: a list of assessment-metadata
records, with attributes \code{registry_ref}, \code{registry_root}, and \code{jurisdiction}.
}
\description{
Reads the registry for one \code{jurisdiction}, optionally filtered to a \code{system}
and/or \code{year}, and returns the matching assessment-system records. When
\code{attach_targets = TRUE} (the default), accountability achievement targets are
resolved and merged onto each assessment record under \code{achievement_targets}
(keyed by content area) -- the shape downstream consumers such as SGPc expect
-- even though targets are authored separately in accountability records
(ADR-002).
}
\details{
The returned object carries the registry commit SHA in its \code{"registry_ref"}
attribute (see \code{\link[=amrr_registry_ref]{amrr_registry_ref()}}); record it in analysis output to make
the run reproducible.
}
\examples{
\dontrun{
# From a local checkout. With R started inside the checkout, no registry
# argument is needed (amrr discovers it from the working directory); otherwise
# pass registry = or set options(amrr.registry=).
md <- get_metadata("IN", system = "wida-access", year = 2024)
amrr_registry_ref(md)
amrr_targets(md[[1]], "ELP_COMPOSITE")

# Reproducible remote: canonical sidecars straight from GitHub, pinned by SHA:
md <- get_metadata("IN", system = "ilearn", year = 2024,
                   registry = "github://CenterForAssessment/assessment-metadata-registry",
                   ref = "b824b20")
amrr_registry_ref(md)   # the resolved 40-hex commit SHA -- the reproducibility pin

# Convenience (latest build only): the published derived layer over HTTP:
pages <- "https://centerforassessment.github.io/assessment-metadata-registry"
md <- get_metadata("IN", system = "ilearn", year = 2024, registry = pages)
}
}
