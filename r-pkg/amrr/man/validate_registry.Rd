% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_registry}
\alias{validate_registry}
\title{Validate every registry sidecar (Tier A gate)}
\usage{
validate_registry(
  registry = NULL,
  schema_dir = NULL,
  quiet = FALSE,
  error = TRUE
)
}
\arguments{
\item{registry}{Path to a registry checkout (the directory containing
\verb{metadata/} and \verb{schemas/}). Defaults to \code{option("amrr.registry")} then the
\code{AMRR_REGISTRY} environment variable (see \code{\link[=get_metadata]{get_metadata()}}).}

\item{schema_dir}{Directory of JSON Schemas. Defaults to \verb{<registry>/schemas}.}

\item{quiet}{Suppress the per-file \code{ok}/\code{FAIL} output.}

\item{error}{If \code{TRUE} (default), stop with an error when any file fails, so a
CLI call (\verb{Rscript -e 'amrr::validate_registry(".")'}) exits non-zero. Set
\code{FALSE} to return the report instead.}
}
\value{
Invisibly, a list with \code{n_files}, \code{n_errors}, and \code{results} (a named
list mapping each failing file to its error messages).
}
\description{
Checks each authored sidecar under \verb{<registry>/metadata/} against its JSON
Schema (via \pkg{jsonvalidate}) plus the registry invariants a schema cannot
express: filename/path identity, cutscore count vs. achievement levels,
monotonic cutscores, accountability cross-links, and a \code{source_citation} on
non-draft records.
}
\examples{
\dontrun{
amrr::validate_registry(".")
}
}
