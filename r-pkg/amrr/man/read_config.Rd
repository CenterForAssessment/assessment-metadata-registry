% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{read_config}
\alias{read_config}
\title{Expand a compact assessment-config back into a v2 record (ADR-010)}
\usage{
read_config(config)
}
\arguments{
\item{config}{A config list from \code{\link[=as_config]{as_config()}} (or the colleague's
\code{amr.assessment_config.v1} shape).}
}
\value{
A single \code{amr.assessment.v2} assessment record (a named list).
}
\description{
The inverse of \code{\link[=as_config]{as_config()}}: reconstructs the canonical \code{amr.assessment.v2}
assessment record for the config's snapshot year -- resolving \code{level_scheme}
references, splitting unified \code{cuts} back into \code{cutscores} + \code{scale_bounds},
and rebuilding each content area's \code{enrollment} from the \code{map}. Returns an
in-memory record (it does not write a sidecar); blocks the config view drops
(\verb{measurement.*}, provenance, comparability, source_documents) are not
reconstructed, so a config round-trip preserves the \emph{core} facts, not byte
identity. Validate the result with \code{\link[=validate_registry]{validate_registry()}} before authoring.
}
\seealso{
\code{\link[=as_config]{as_config()}}
}
