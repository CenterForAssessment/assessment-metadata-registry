% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{as_config}
\alias{as_config}
\title{Project registry records into the compact assessment-config view (ADR-010)}
\usage{
as_config(x, year = NULL)
}
\arguments{
\item{x}{An \code{amrr_metadata} set or list of assessment records for a single
jurisdiction and assessment system (multiple years allowed).}

\item{year}{Snapshot year for \code{tests}/\code{cuts} (default: the latest present).}
}
\value{
A named list with the config shape (\code{schema_version}, \code{jurisdiction},
\code{program}, \code{level_schemes}, \code{tests}, \code{map}, \code{years}).
}
\description{
Renders one jurisdiction x assessment-system's records into the compact, DRY
authoring shape from the colleague spec (\code{amr.assessment_config.v1}): reusable
named \code{level_schemes}, \code{tests} keyed by content area, a
\code{content_area -> grade -> test} \code{map}, and unified per-grade \code{cuts}
(\verb{\{loss, hoss, values\}}). This is a \emph{lens} on the canonical v2 sidecars, not a
replacement -- extension blocks (\verb{measurement.*}), provenance, comparability,
and source documents are intentionally dropped. Use \code{\link[=read_config]{read_config()}} for the
inverse.
}
\details{
A config describes one program (= one assessment system). Cuts/levels are a
single-year snapshot; \code{years$tested_years} records the full span present.
}
\seealso{
\code{\link[=read_config]{read_config()}}
}
