% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/migrate.R
\name{migrate_registry}
\alias{migrate_registry}
\title{Migrate Tier A sidecars from v1 to v2 (ADR-009)}
\usage{
migrate_registry(registry = NULL, to = "v2", write = TRUE, quiet = FALSE)
}
\arguments{
\item{registry}{Path to a registry checkout (the directory containing
\verb{metadata/}). Defaults to \code{option("amrr.registry")} then \code{AMRR_REGISTRY}.}

\item{to}{Target schema generation; only \code{"v2"} is supported.}

\item{write}{If \code{TRUE} (default), rewrite the sidecar files in place. Set
\code{FALSE} for a dry run (report what would change without touching files).}

\item{quiet}{Suppress the per-file output.}
}
\value{
Invisibly, a list with \code{n_migrated}, \code{n_skipped} (already v2), and
\code{files} (relative paths rewritten or, dry-run, that would be).
}
\description{
Mechanically restamps every v1 sidecar under \verb{<registry>/metadata/} to the v2
schemas: \code{schema_version} -> \code{amr.assessment.v2} / \code{amr.accountability.v2},
\code{assessment_type} normalized to the canonical enum, and each content area's
required \code{enrollment} block seeded from its cutscore grade keys
(\code{intended_enrollment_grade} = \code{"variable"} for \code{elp} systems, \code{"fixed"}
otherwise -- review the seeded values during authoring). Optional v2 fields a
v1 record cannot supply (\code{scale_bounds}, \verb{measurement.*}, \code{source_documents})
are left absent: migration never invents facts.
}
\details{
Intended use: run once, review the diff, commit the whole corpus in one
reviewed commit, then run \code{\link[=validate_registry]{validate_registry()}} (ADR-009 D6).
}
\examples{
\dontrun{
amrr::migrate_registry(".", write = FALSE)  # dry run
amrr::migrate_registry(".")                 # migrate, then review the diff
}
}
